#include <iostream>

class Parser {
public:
    Parser();
    bool parse(std::istream &);
private:
};



bool Parser::parse(std::istream & in)
{
    std::stringstream ss1;
    std::stringstream ss2

    std::string str;

    in >> str;
    ss1<< str;

    while(){}
}
