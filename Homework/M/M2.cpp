#include <iostream>
#include <sstream>


int
main()
{
    std::string cur_str;
    Parser parser;

    while (std::getline(std::cin, line)) {
        std::stringstream ss;
        ss << line;
        if (p
    }
    return 0;
}
